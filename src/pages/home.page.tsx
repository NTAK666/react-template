import { FC } from 'react';

type HomePageProps = {};

const HomePage: FC<HomePageProps> = ({}) => {
  return (
    <div>
      <p>This is a simple React App Quickstart Template.</p>
    </div>
  );
};

export default HomePage;
